/**
 * The console route.
 *
 * The frame it renders lives with the rest of the console rather than here,
 * because the sidebar, the bar and the scrolling work region are one thing and
 * a route file is not the place for it.
 */
export { default } from './console/ConsoleFrame'
