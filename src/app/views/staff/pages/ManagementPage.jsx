import StaffScreen from '../StaffScreen'
import ShiftBoard from '../parts/ShiftBoard'

/** The day's figures and the desk, on their own. */
export default function ManagementPage() {
  return <ShiftBoard Shell={StaffScreen} />
}
