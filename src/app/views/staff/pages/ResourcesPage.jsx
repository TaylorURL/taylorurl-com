import StaffScreen from '../StaffScreen'
import Handbook from '../parts/Handbook'

/** The handbook, on its own, at the site's reading measure. */
export default function ResourcesPage() {
  return <Handbook Shell={StaffScreen} />
}
