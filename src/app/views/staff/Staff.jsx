/**
 * The staff portal route.
 *
 * The frame it renders lives beside the screens it holds rather than here,
 * because the head, the scroll and the foot are one thing and a route file is
 * not the place for it.
 */
export { default } from './StaffFrame'
