// CursorGlow has been retired — the redesigned site uses restraint over
// ambient cursor effects to keep the cinematic, defense-tech tone. Kept as a
// no-op so any stale import resolves cleanly without rendering anything.
export default function CursorGlow() {
  return null
}
