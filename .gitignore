logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
node_modules
dist
dist-ssr
*.local
.vscode/
.idea/
# Agent session state is per-machine, but the startup hook and the settings
# that register it have to reach a fresh clone: they are what points git at
# .githooks/ and gives the session an identity that is not an AI vendor's.
.claude/*
!.claude/settings.json
!.claude/hooks/
.claude/hooks/*
!.claude/hooks/session-start.sh
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
key/
*.key
.env
.env.*
!.env.example
.intelligence/
INTELLIGENCE.md
*.iml
.fleet/
.junie/
# Schema, migrations, dumps and one-off queries are held privately and never
# tracked here, so the exclusion carries no exceptions.
*.sql
supabase/
*.pem
*.p12
*.pfx
id_rsa
id_ed25519
.npmrc
.envrc

# Vercel CLI link state (project/org ids) is per-machine.
.vercel

# Windows folder metadata
desktop.ini

# The status board capture tool, compiled from scripts/status-board-shot.swift
# on first use.
scripts/.status-board-shot
brand/social/out/

# Written by Python the first time a card generator imports the drawing library
# beside it.
__pycache__/

# Build and test caches
coverage/
test-results/
.eslintcache
*.tsbuildinfo
